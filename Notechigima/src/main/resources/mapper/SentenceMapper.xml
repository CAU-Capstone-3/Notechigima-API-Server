<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.capstone.notechigima.repository.SentenceRepository">

    <select id="getSentenceListByNoteId" resultType="com.capstone.notechigima.domain.sentenceity">
        SELECT *
        FROM sentence_unit s
        WHERE #{noteId} = s.note_id
    </select>

    <select id="getSentenceListByTopicId" resultType="com.capstone.notechigima.domain.sentence.SentenceWithWriterEntity">
        SELECT s.sentence_id sentenceId, s.content content,
               u.user_id writerId, u.nickname writerName
        FROM sentence_unit s
        LEFT JOIN note n ON s.note_id = n.note_id
        LEFT JOIN user u ON n.owner_id = u.user_id
        WHERE #{topicId} = n.topic_id
    </select>

    <insert id="insertAll" parameterType="java.util.Map">
        INSERT INTO sentence_unit(note_id, content, sentence_type, sequence_num)
        VALUES
            <foreach collection="list" item="item" separator=" , ">
                (#{item.noteId}, #{item.content}, #{item.sentenceType}, #{item.sequenceNum})
            </foreach>
    </insert>
</mapper>